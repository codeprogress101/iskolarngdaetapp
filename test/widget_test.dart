import 'package:flutter_test/flutter_test.dart';

import 'package:ldsp_applicant/app/app.dart';

void main() {
  test('ApplicantApp widget can be constructed', () {
    expect(const ApplicantApp(), isA<ApplicantApp>());
  });
}
